apiVersion: v1
kind: Service
metadata:
  name: {{ include "dominoes-game.fullname" . }}
  labels:
    {{- include "dominoes-game.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dominoes-game.selectorLabels" . | nindent 4 }}